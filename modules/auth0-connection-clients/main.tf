# TODO: auth0_connection_client resource (connection_id, client_id via var)
# links one connection to one app - call this module once per (connection, app) pair, or loop with for_each on a list of pairs
